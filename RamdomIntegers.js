// Random integer

// Get random decimal number between 0 and 0.99...and save it to a variable
var decimal = Marth.random() * 10;

// Rounding up the value of decimal
var rounded = Math.ceil(decimal);

//Log the value of rounded to the console
console.log(rounded);